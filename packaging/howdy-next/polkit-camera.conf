[Service]
# polkit 127 isolates the PAM helper from /dev/video* by default. Howdy needs
# access to the IR V4L2 node during a polkit authentication prompt.
PrivateDevices=no
DeviceAllow=char-video4linux rw
